<template>
  <style>
    #container {
      flexDirection: row;
      alignItems: center;
      backgroundColor: #eeeeee;
      padding: 5;
    }
    #count {
      fontSize: 10;
    }
    #filters {
      flexDirection: row;
      flexGrow: 1;
      paddingLeft: 5;
      paddingRight: 5;
      justifyContent: center;
    }
    #filters button {
      titleColor: #000000;
      titleFontSize: 10;
      borderColor: #000000;
      cornerRadius: 5;
      padding: 5;
      marginLeft: 5;
      selectedBackgroundColor: #ff0000;
    }
    #clearAll {
      titleColor: #000000;
      titleFontSize: 10;
    }
  </style>
  <box id="container">
    <text text="$count" id="count" />
    <box id="filters">
      <button title="all" onTap="handleSelectAll" selected="$allSelected" />
      <button title="active" onTap="handleSelectActive" selected="$activeSelected" />
      <button title="completed" onTap="handleSelectCompleted" selected="$completedSelected"/>
    </box>
    <button title="clear all" onTap="handleClearCompleted" />
  </box>
</template>
